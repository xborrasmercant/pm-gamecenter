<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Define the solid color of the button -->
    <solid android:color="@color/dim_black" />

    <!-- Define the border properties -->
    <stroke
        android:width="2dp"
        android:color="@color/orange_neon" />

    <!-- Optionally define the corner radius -->
    <corners android:radius="4dp" />
</shape>
